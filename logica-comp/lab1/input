\documentclass[16pt]{report}
\pagestyle{empty}
\begin{document}
$$(((p \rightarrow q) \vee (p \rightarrow \neg(p \rightarrow r))) \wedge (r \rightarrow p)) \wedge (\neg p \rightarrow q)$$
\end{document}